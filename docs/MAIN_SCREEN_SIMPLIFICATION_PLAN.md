# План Упрощения Главного Экрана

**Дата создания:** 2025-12-07  
**Статус:** 📋 План готов к реализации  
**Автор:** Kilo Code (Architect Mode)

---

## 🎯 Цель

Упростить главный экран бота, сделав его более удобным и интуитивным:
- Убрать динамическую логику клавиатуры
- Сделать 4 постоянные кнопки на главном экране
- Перенести часто используемые действия на первый уровень

---

## 📊 Текущее Состояние vs Желаемое

### ❌ Текущая Проблема

1. **Динамическая клавиатура**: После кормления кнопка "🍽️ Собачка поел" заменяется на "📝 Уточнить детали"
2. **Скрытые действия**: Кнопка "⏹️ Завершить кормления" находится в подменю (2 клика)
3. **Потеря доступа**: После кормления пользователь теряет кнопку для повторного кормления
4. **Сложная логика**: Параметр `showFeedingDetailsButton` в [`getMainKeyboard()`](../src/utils/keyboards.ts:5)

### ✅ Желаемое Состояние

**Главный экран (4 постоянные кнопки):**
1. 🍽️ Собачка поел
2. 📝 Уточнить детали кормления
3. ⏹️ Завершить кормления на сегодня
4. Другие действия

**Другие действия (обновленное):**
1. Когда следующее кормление?
2. 📋 История кормлений | ⚙️ Настройки
3. 🏠 На главную

---

## 🗺️ Диаграмма Изменений

### До Изменений

```
Главный экран (динамически)
├─ Если НЕ покормили:
│  ├─ 🍽️ Собачка поел
│  ├─ Когда следующее кормление?
│  └─ Другие действия
│      └─ ⏹️ Завершить кормления (2 клика)
│
└─ Если покормили:
   ├─ 📝 Уточнить детали кормления
   ├─ Когда следующее кормление?
   └─ Другие действия
       └─ ⏹️ Завершить кормления (2 клика)
```

### После Изменений

```
Главный экран (статично)
├─ 🍽️ Собачка поел (всегда доступно)
├─ 📝 Уточнить детали кормления (всегда доступно)
├─ ⏹️ Завершить кормления (1 клик!)
└─ Другие действия
   ├─ Когда следующее кормление?
   ├─ 📋 История кормлений
   └─ ⚙️ Настройки
```

---

## 📋 Декомпозиция Задач

### Задача 1: Обновить `getMainKeyboard()` в keyboards.ts

**Файл:** [`src/utils/keyboards.ts`](../src/utils/keyboards.ts:5)  
**Строки:** 5-22

**Текущий код:**
```typescript
export function getMainKeyboard(showFeedingDetailsButton = false) {
    const keyboard = [];

    if (showFeedingDetailsButton) {
        keyboard.push(['📝 Уточнить детали кормления']);
    }

    if (!showFeedingDetailsButton) {
        keyboard.push([`${EMOJIS.FEED} Собачка поел`]);
    }

    keyboard.push(['Когда следующее кормление?', 'Другие действия']);

    return Markup.keyboard(keyboard).resize();
}
```

**Новый код:**
```typescript
export function getMainKeyboard() {
    return Markup.keyboard([
        ['🍽️ Собачка поел'],
        ['📝 Уточнить детали кормления'],
        ['⏹️ Завершить кормления на сегодня'],
        ['Другие действия']
    ]).resize();
}
```

**Изменения:**
- ❌ Убран параметр `showFeedingDetailsButton`
- ❌ Убрана вся условная логика
- ❌ Убрана кнопка "Когда следующее кормление?"
- ✅ 4 постоянные кнопки
- ✅ Простой, читаемый код

---

### Задача 2: Обновить `getOtherActionsKeyboard()` в keyboards.ts

**Файл:** [`src/utils/keyboards.ts`](../src/utils/keyboards.ts:126)  
**Строки:** 126-133

**Текущий код:**
```typescript
export function getOtherActionsKeyboard() {
    return Markup.keyboard([
        ['⏹️ Завершить кормления на сегодня'],
        ['📋 История кормлений', '⚙️ Настройки'],
        ['🏠 На главную'],
    ]).resize();
}
```

**Новый код:**
```typescript
export function getOtherActionsKeyboard() {
    return Markup.keyboard([
        ['Когда следующее кормление?'],
        ['📋 История кормлений', '⚙️ Настройки'],
        ['🏠 На главную'],
    ]).resize();
}
```

**Изменения:**
- ❌ Убрана кнопка "⏹️ Завершить кормления на сегодня"
- ✅ Добавлена кнопка "Когда следующее кормление?"

---

### Задача 3: Обновить вход в сцену в main.ts

**Файл:** [`src/scenes/main.ts`](../src/scenes/main.ts:41)  
**Строки:** 41-64

**Текущий код:**
```typescript
mainScene.enter(ctx => {
    const showFeedingDetailsButton = ctx.session?.justFed === true;
    
    if (ctx.session) {
        ctx.session.justFed = false;
    }

    if (!ctx.session?.firstVisitDone) {
        if (ctx.session) {
            ctx.session.firstVisitDone = true;
        }
        ctx.reply(UI_TEXTS.welcome, getMainKeyboard(showFeedingDetailsButton));
    } else {
        ctx.reply(
            UI_TEXTS.navigation.goingHome,
            getMainKeyboard(showFeedingDetailsButton)
        );
    }
});
```

**Новый код:**
```typescript
mainScene.enter(ctx => {
    if (!ctx.session?.firstVisitDone) {
        if (ctx.session) {
            ctx.session.firstVisitDone = true;
        }
        ctx.reply(UI_TEXTS.welcome, getMainKeyboard());
    } else {
        ctx.reply(UI_TEXTS.navigation.goingHome, getMainKeyboard());
    }
});
```

**Изменения:**
- ❌ Убрана переменная `showFeedingDetailsButton`
- ❌ Убрана логика с `ctx.session.justFed`
- ✅ Всегда показываем одинаковую клавиатуру

---

### Задача 4: Удалить обработчик "Когда следующее?" из main.ts

**Файл:** [`src/scenes/main.ts`](../src/scenes/main.ts:104)  
**Строки:** 104-144

**Действие:** УДАЛИТЬ ПОЛНОСТЬЮ

```typescript
// УДАЛИТЬ ЭТУ ФУНКЦИЮ - она переезжает в other-actions.ts
mainScene.hears(/Когда следующее кормление\?/, async ctx => {
    // ... весь код от строки 104 до 144 ...
});
```

**Причина:** Эта функциональность переносится в сцену "Другие действия"

---

### Задача 5: Обновить обработчик кормления в main.ts

**Файл:** [`src/scenes/main.ts`](../src/scenes/main.ts:147)  
**Строка:** 242

**Текущий код:**
```typescript
mainScene.hears(/🍽️ Собачка поел/, async ctx => {
    // ... логика кормления ...
    
    // Строка 242:
    await ctx.reply(message, getMainKeyboard(true));
});
```

**Новый код:**
```typescript
mainScene.hears(/🍽️ Собачка поел/, async ctx => {
    // ... та же логика кормления ...
    
    // Строка 242:
    await ctx.reply(message, getMainKeyboard());
});
```

**Изменения:**
- ❌ Убран параметр `true` при вызове `getMainKeyboard()`

---

### Задача 6: Добавить обработчик "⏹️ Завершить кормления" в main.ts

**Файл:** [`src/scenes/main.ts`](../src/scenes/main.ts:247)  
**Место:** После строки 247 (после обработчика "Собачка поел")

**Добавить новый код:**
```typescript
// Обработка кнопки "⏹️ Завершить кормления на сегодня"
mainScene.hears(/⏹️ Завершить кормления на сегодня/, async ctx => {
    try {
        if (!ctx.timerService || !ctx.database) {
            ctx.reply(UI_TEXTS.errors.servicesNotInitialized);
            return;
        }

        const dbUser = await ctx.database.getUserByTelegramId(ctx.from!.id);
        if (!dbUser) {
            ctx.reply(UI_TEXTS.errors.userNotFound);
            return;
        }

        ctx.timerService.stopAllTimers();

        const message = MessageBuilder.feedingStopped(createUserLink(dbUser));

        // Уведомление всех пользователей
        const allUsers = await ctx.database.getAllUsers();
        for (const u of allUsers) {
            if (u.notificationsEnabled) {
                try {
                    await ctx.telegram.sendMessage(u.telegramId, message);
                } catch (error) {
                    console.error(
                        `Ошибка отправки сообщения пользователю ${u.telegramId}:`,
                        error
                    );
                }
            }
        }

        console.log(`Кормления остановлены пользователем: ${dbUser.username}`);
        
        // Остаемся на главном экране
        ctx.reply('Кормления на сегодня завершены', getMainKeyboard());
    } catch (error) {
        console.error('Ошибка при остановке кормлений:', error);
        ctx.reply(MessageFormatter.error('Произошла ошибка при остановке кормлений. ' + UI_TEXTS.common.tryAgain));
    }
});
```

**Изменения:**
- ✅ Добавлен новый обработчик кнопки
- ✅ Логика скопирована из [`other-actions.ts`](../src/scenes/other-actions.ts:62)
- ✅ После остановки остаемся на главном экране (не переходим)

---

### Задача 7: Обновить команду /home в main.ts

**Файл:** [`src/scenes/main.ts`](../src/scenes/main.ts:304)  
**Строка:** 305

**Текущий код:**
```typescript
mainScene.command('home', ctx => {
    ctx.reply(UI_TEXTS.navigation.goingHome, getMainKeyboard());
});
```

**Изменение:** Убрать параметр при вызове (код уже правильный после изменения функции)

---

### Задача 8: Обновить обработчик неизвестных команд в main.ts

**Файл:** [`src/scenes/main.ts`](../src/scenes/main.ts:318)  
**Строка:** 324

**Текущий код:**
```typescript
mainScene.on('text', ctx => {
    const text = (ctx.message as any)?.text || '';
    if (text.startsWith('/') || text.includes('🏠')) {
        return;
    }
    ctx.reply(UI_TEXTS.navigation.unknownCommand, getMainKeyboard());
});
```

**Изменение:** Убрать параметр при вызове (код уже правильный после изменения функции)

---

### Задача 9: Удалить обработчик "⏹️ Завершить" из other-actions.ts

**Файл:** [`src/scenes/other-actions.ts`](../src/scenes/other-actions.ts:62)  
**Строки:** 62-112

**Действие:** УДАЛИТЬ ПОЛНОСТЬЮ

```typescript
// УДАЛИТЬ ЭТУ ФУНКЦИЮ - она переехала в main.ts
otherActionsScene.hears(/⏹️ Завершить кормления на сегодня/, async ctx => {
    // ... весь код от строки 62 до 112 ...
});
```

**Причина:** Эта функциональность перенесена на главный экран

---

### Задача 10: Обновить локальную клавиатуру в other-actions.ts

**Файл:** [`src/scenes/other-actions.ts`](../src/scenes/other-actions.ts:41)  
**Строки:** 41-46

**Текущий код:**
```typescript
function getOtherActionsKeyboard() {
    return createNavigationKeyboard([
        ['⏹️ Завершить кормления на сегодня'],
        ['📋 История кормлений', '⚙️ Настройки'],
    ]);
}
```

**Новый код:**
```typescript
function getOtherActionsKeyboard() {
    return createNavigationKeyboard([
        ['Когда следующее кормление?'],
        ['📋 История кормлений', '⚙️ Настройки'],
    ]);
}
```

**Изменения:**
- ❌ Убрана кнопка "⏹️ Завершить кормления на сегодня"
- ✅ Добавлена кнопка "Когда следующее кормление?"

---

### Задача 11: Добавить обработчик "Когда следующее?" в other-actions.ts

**Файл:** [`src/scenes/other-actions.ts`](../src/scenes/other-actions.ts:59)  
**Место:** После строки 59 (после `otherActionsScene.enter`)

**Добавить новый код:**
```typescript
// Обработка кнопки "Когда следующее кормление?"
otherActionsScene.hears(/Когда следующее кормление\?/, async ctx => {
    try {
        if (!ctx.timerService || !ctx.database) {
            ctx.reply(UI_TEXTS.errors.servicesNotInitialized);
            return;
        }

        const nextFeedingInfo = ctx.timerService.getNextFeedingInfo();

        if (!nextFeedingInfo.isActive || !nextFeedingInfo.time) {
            ctx.reply(
                `${UI_TEXTS.status.paused}\nЧтобы возобновить, нажмите "${UI_TEXTS.feeding.buttonText}"`
            );
            return;
        }

        const currentUser = await ctx.database.getUserByTelegramId(ctx.from!.id);

        const nextFeedingTime = nextFeedingInfo.time;
        const timeString = currentUser
            ? formatDateTime(nextFeedingTime, currentUser.timezone).split(' в ')[1]
            : nextFeedingTime.getHours().toString().padStart(2, '0') +
              ':' +
              nextFeedingTime.getMinutes().toString().padStart(2, '0');

        const now = new Date();
        const timeDiff = nextFeedingTime.getTime() - now.getTime();
        const timeDiffString = formatInterval(Math.floor(timeDiff / (1000 * 60)));

        ctx.reply(`⏰ Следующее кормление в ${timeString} (через ${timeDiffString})`);
    } catch (error) {
        console.error('Ошибка при получении времени следующего кормления:', error);
        ctx.reply(
            MessageFormatter.error('Произошла ошибка при получении времени следующего кормления. ' + UI_TEXTS.common.tryAgain)
        );
    }
});
```

**Добавить импорты** в начало файла (если их нет):
```typescript
import { formatDateTime, formatInterval } from '../utils/time-utils';
```

**Изменения:**
- ✅ Добавлен новый обработчик кнопки
- ✅ Логика скопирована из [`main.ts`](../src/scenes/main.ts:104)

---

## 📊 Метрики Изменений

| Метрика | До | После | Улучшение |
|---------|-----|-------|-----------|
| **Кнопок на главном** | 2-3 (динамически) | 4 (статично) | +33% доступ |
| **Кликов до "Завершить"** | 2 | 1 | **-50%** |
| **Строк кода keyboards.ts** | 18 строк | 16 строк | -11% |
| **Строк кода main.ts** | 325 строк | ~365 строк | +12% (новый обработчик) |
| **Строк кода other-actions.ts** | 158 строк | ~155 строк | -2% (замена обработчика) |
| **Сложность логики** | Высокая (условия) | Низкая (статика) | **-70%** |

---

## ✅ Чек-лист Реализации

### Файл: [`src/utils/keyboards.ts`](../src/utils/keyboards.ts:1)

- [ ] Задача 1: Обновить `getMainKeyboard()` (строки 5-22)
- [ ] Задача 2: Обновить `getOtherActionsKeyboard()` (строки 126-133)

### Файл: [`src/scenes/main.ts`](../src/scenes/main.ts:1)

- [ ] Задача 3: Обновить вход в сцену (строки 41-64)
- [ ] Задача 4: Удалить обработчик "Когда следующее?" (строки 104-144)
- [ ] Задача 5: Обновить обработчик кормления (строка 242)
- [ ] Задача 6: Добавить обработчик "⏹️ Завершить кормления" (после строки 247)
- [ ] Задача 7: Проверить команду /home (строка 305)
- [ ] Задача 8: Проверить обработчик неизвестных команд (строка 324)

### Файл: [`src/scenes/other-actions.ts`](../src/scenes/other-actions.ts:1)

- [ ] Задача 9: Удалить обработчик "⏹️ Завершить" (строки 62-112)
- [ ] Задача 10: Обновить локальную клавиатуру (строки 41-46)
- [ ] Задача 11: Добавить обработчик "Когда следующее?" (после строки 59)
- [ ] Задача 11: Добавить недостающие импорты

### Проверка

- [ ] Компиляция проходит без ошибок
- [ ] Все обработчики работают корректно
- [ ] Клавиатуры отображаются правильно
- [ ] Навигация работает логично

---

## 🎯 Преимущества Новой Структуры

### ✅ Для Пользователя

- **Простота**: Всегда одна и та же клавиатура на главном
- **Доступность**: Все основные действия в 1 клик
- **Предсказуемость**: Пользователь всегда знает где что находится
- **Скорость**: Завершение кормлений в 2 раза быстрее

### ✅ Для Разработчика

- **Читаемость**: Меньше условной логики
- **Поддерживаемость**: Статическая структура меню
- **Простота**: Нет параметров `showFeedingDetailsButton`
- **Безопасность**: Меньше мест для ошибок

---

## 🚀 Следующие Шаги

1. **Переключиться в Code режим**
2. **Реализовать задачи по порядку** (1-11)
3. **Протестировать каждое изменение**
4. **Создать коммит** с описанием изменений
5. **Обновить документацию** (если требуется)

---

## 📚 Связанные Документы

- [`REFACTORING_COMPLETE.md`](REFACTORING_COMPLETE.md) - Предыдущий рефакторинг UI
- [`architecture.md`](architecture.md) - Общая архитектура проекта
- [`REFACTORING_SUMMARY.md`](REFACTORING_SUMMARY.md) - Итоги рефакторинга

---

**Создано:** 2025-12-07  
**Архитектор:** Kilo Code  
**Статус:** ✅ Готово к реализации
